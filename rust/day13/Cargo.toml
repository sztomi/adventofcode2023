[package]
name = "day13"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
common = { path = "../common" }
dbg-pls = { version = "0.4.3", features = ["colors", "pretty"] }
itertools = "*"
